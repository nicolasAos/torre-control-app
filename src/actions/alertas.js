import api from '../services/apiTorreControl';

export const sendRegisterNews = ({
  tractor,
  viaje,
  nombreOperador,
  telefonoOperador,
  idNovedad,
  userId,
}) =>
  new Promise((resolve, reject) => {

  });
